import { css } from '@emotion/react'

const GlobalStyle = css`
  * {
    margin: 0;
    padding: 0;
    list-style: none;
    box-sizing: border-box;
  }
  body,
  html,
  #root {
    width: 100%;
  }
`

export default GlobalStyle
